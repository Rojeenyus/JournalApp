<div class="container">
  <h1>To Do List</h1>
  <%= link_to "New Category", new_category_path %>
  <ul>
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.name, category %>
      </li>
    <% end %>
  </ul>
</div>
